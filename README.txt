Shooter3D

Created animation clips from mocap data. 
Used clips to create an animation controller for player character.


Learned:
Create animation clips from mocap data.
Use downloaded animations and smooth their transitions using blend trees. 
Animation controller.
Camera follows character and camera collisons to move camera. 

Assets:
Models from unity store and mixamo.
Animations from unity store and mixamo.


Bug List:
Jumping is very janky (not a smooth animation)
Can jump continually. 
Can fall through floor b/c jumping code is temp
Gun isn't positioned correctly